apiVersion: v1
kind: Service
metadata:
  name: veterinaria-api
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-type: "nlb"
spec:
  type: LoadBalancer

  selector:
    app: veterinaria-api

  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000